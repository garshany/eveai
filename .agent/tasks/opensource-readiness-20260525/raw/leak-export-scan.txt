$ rg private leak scan current public surface
$ scripts/export-public.sh /tmp/eveai-public-export-check
Public export written to: /tmp/eveai-public-export-check

Next steps:
1. Inspect the export.
2. Run a secret scan over the export.
3. Initialize a new git repository inside the export.
4. Push that new repository publicly only after rotating exposed credentials.
$ rg private leak scan export
